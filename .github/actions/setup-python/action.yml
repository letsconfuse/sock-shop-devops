---
name: Setup Python
description: Setup Python and pip dependencies

runs:
  using: composite
  steps:
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install Python tools
      shell: bash
      run: |
        python -m pip install --upgrade pip
        pip install yamllint
