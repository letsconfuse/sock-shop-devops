---
name: Setup Node
description: Setup Node.js and markdownlint

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: "22"

    - shell: bash
      run: npm install -g markdownlint-cli
